#include<bits/stdc++.h>
using namespace std;
int main()
{
   string s,t;
   int l,i,count=0;
   cin>>s>>t;
   l=t.size();
   for(i=0 ; i<l ; i++)
   {
       if(s[count]==t[i])
        count++;
   }
   cout<<count+1;
    return 0;
}
