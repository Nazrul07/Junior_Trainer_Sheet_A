#include<bits/stdc++.h>
using namespace std;
int main()
{
   int n,k,j=0,i;
   char c;
   cin>>n>>k;
   while(j<=n)
   {
       for(i=1 ; i<=k ; i++)
       {
           j++;
           if(j>n)break;
           int temp=i+96;
           c=temp;
           cout<<c;
       }
   }
    return 0;
}
