#include <iostream>
using namespace std;
int main() 
{
 int n;
 cin>>n;
 int i,h[n],a[n],j,c=0;
 for(i=0 ; i<n ; i++)
 {
     cin>>h[i]>>a[i];
 }
 for(i=0 ; i<n ; i++)
 {
     for(j=0 ; j<n ; j++)
     {
         if(h[i]==a[j])
           c++;
     }
 }
 cout<<c<<endl;
 return 0;
}
